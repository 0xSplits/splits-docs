diff --git a/dist/internal/llms.d.ts b/dist/internal/llms.d.ts
index 42a1b6d82f03226961d8a1ad91160620139e7518..f5a1896260e404f10b07620ab773267524f5a3f9 100644
--- a/dist/internal/llms.d.ts
+++ b/dist/internal/llms.d.ts
@@ -19,6 +19,7 @@ export declare function buildLlmsContent(options: buildLlmsContent.Options): Pro
 }>;
 export declare namespace buildLlmsContent {
     type Options = {
+        basePath?: string | undefined;
         pages: Page[];
         title: string;
         description?: string | undefined;
diff --git a/dist/internal/llms.js b/dist/internal/llms.js
index 6ad2b91c6b8a0a9e64fff6a06da5efa9c56ff0ae..70cfae0441623c1c2b79f9ee248945fdc691e650 100644
--- a/dist/internal/llms.js
+++ b/dist/internal/llms.js
@@ -6,7 +6,7 @@ import { unified } from 'unified';
 import * as MarkdownImports from './markdown-imports.js';
 import * as OpenApiMarkdown from './openapi/markdown.js';
 export async function buildLlmsContent(options) {
-    const { title, description, rehypePlugins, remarkPlugins, sidebar } = options;
+    const { basePath = '/', title, description, rehypePlugins, remarkPlugins, sidebar } = options;
     // Dedupe by path (first occurrence wins), so consumer-authored source pages
     // take precedence over generated pages (e.g. OpenAPI) mounted at the same path.
     const seen = new Set();
@@ -59,12 +59,25 @@ export async function buildLlmsContent(options) {
     }))
         .then((data) => data.filter((data) => data !== null))
         .then((data) => sortResults(data, sidebar));
+    const prefix = basePath.replace(/\/$/, '');
+    // Idempotent: a path already under the base path is left alone, so authored
+    // `/docs/…` links (and re-runs) never end up as `/docs/docs/…`.
+    const prefixPath = (path) => basePath === '/' || path === prefix || path.startsWith(`${prefix}/`)
+        ? path
+        : `${prefix}${path}`;
+    const prefixLinks = (content) => basePath === '/'
+        ? content
+        : content
+            .replace(/(\]\()(\/(?!\/)[^)\s]*)/g, (_match, open, path) => `${open}${prefixPath(path)}`)
+            .replace(/\b(href|src)=(["'])(\/(?!\/)[^"']*)/g, (_match, attr, quote, path) => `${attr}=${quote}${prefixPath(path)}`);
+    for (const result of results)
+        result.content = prefixLinks(result.content);
     const llmsTxtContent = [`# ${title}`, ''];
     if (description)
         llmsTxtContent.push(description, '');
     const nav = [];
     for (const { title, description, path } of results)
-        nav.push(`- [${title}](${path === '/' ? '/index' : path})${description ? `: ${description}` : ''}`);
+        nav.push(`- [${title}](${prefixPath(path === '/' ? '/index' : path)})${description ? `: ${description}` : ''}`);
     const sitemap = ['<!--', 'Sitemap:', ...nav, '-->', ''].join('\n');
     const short = [...llmsTxtContent, ...nav];
     const full = [...llmsTxtContent, sitemap, ...results.map((r) => r.content)];
diff --git a/dist/internal/markdown-negotiation.js b/dist/internal/markdown-negotiation.js
index 01c753f7cd9b0043629bd8ff3ea609a6df4f32a6..edba4ef27608ffb45124e77bed34519480bbc25e 100644
--- a/dist/internal/markdown-negotiation.js
+++ b/dist/internal/markdown-negotiation.js
@@ -12,6 +12,8 @@ export const aiUserAgents = [
     'ChatGPT-User/2.0',
     'anthropic-ai',
     'ClaudeBot',
+    'Claude-User',
+    'Claude-SearchBot',
     'claude-web',
     'PerplexityBot',
     'Perplexity-User',
diff --git a/dist/internal/vite-plugins.js b/dist/internal/vite-plugins.js
index 060cff216573fd043d5fd801e18ad581020e3139..e90efe3b5fc547da7cb0e173bdaac52a7d97a8b0 100644
--- a/dist/internal/vite-plugins.js
+++ b/dist/internal/vite-plugins.js
@@ -175,6 +175,7 @@ export function llms(config) {
         // same generated route (`.md` serves the full reference); authored-only guide
         // pages under the section keep their own content.
         return Llms.buildLlmsContent({
+            basePath: config.basePath,
             pages: [...openapiPages, ...pages],
             title,
             description,
@@ -191,16 +192,30 @@ export function llms(config) {
         },
         configureServer(server) {
             let content;
+            // Dev requests arrive under the base path, while generated output is
+            // keyed off the root. Match on the base-path-relative pathname so
+            // `/llms.txt` and the twins resolve under a base path too.
+            const basePath = (config.basePath ?? '/').replace(/\/+$/, '');
+            const toRootPath = (url) => {
+                const rawPath = (url ?? '').split('?')[0];
+                if (!basePath || !rawPath.startsWith(basePath))
+                    return rawPath;
+                const rest = rawPath.slice(basePath.length);
+                if (rest !== '' && !rest.startsWith('/'))
+                    return rawPath;
+                return rest || '/';
+            };
             server.middlewares.use(async (req, res, next) => {
-                if (req.url === '/llms.txt' || req.url === '/llms-full.txt') {
+                const pathname = toRootPath(req.url);
+                if (pathname === '/llms.txt' || pathname === '/llms-full.txt') {
                     content = await buildLlmsContent();
                     res.setHeader('Content-Type', 'text/plain');
-                    res.end(req.url === '/llms.txt' ? content.short : content.full);
+                    res.end(pathname === '/llms.txt' ? content.short : content.full);
                     return;
                 }
-                if (req.url?.startsWith('/assets/md/')) {
+                if (pathname.startsWith('/assets/md/')) {
                     content = await buildLlmsContent();
-                    const pagePath = req.url.slice('/assets/md'.length, -3) || '/';
+                    const pagePath = pathname.slice('/assets/md'.length, -3) || '/';
                     const result = content.results.find((r) => r.path.replace(/\/$/, '') === pagePath.replace(/\/index$/, ''));
                     if (result) {
                         res.setHeader('Content-Type', 'text/markdown; charset=utf-8');
@@ -329,7 +344,7 @@ export function mdx(config) {
  * @returns Plugin.
  */
 export function sitemap(config) {
-    const { baseUrl } = config;
+    const { basePath, baseUrl } = config;
     let built = false;
     let viteConfig;
     function getSiteUrl() {
@@ -340,7 +355,7 @@ export function sitemap(config) {
             'User-agent: *',
             'Allow: /',
             '',
-            `Sitemap: ${siteUrl.replace(/\/$/, '')}/sitemap.xml`,
+            `Sitemap: ${siteUrl.replace(/\/$/, '')}${basePath.replace(/\/$/, '')}/sitemap.xml`,
             '',
         ].join('\n');
     }
@@ -378,7 +393,10 @@ export function sitemap(config) {
                     .replace(/\.(mdx?|tsx?)$/, '')
                     .replace(/\/index$/, '/')
                     .replace(/\/$/, '') || '/';
-                const loc = `${siteUrl.replace(/\/$/, '')}${pagePath}`;
+                // Trailing-slash form: splits.org canonicalizes to it, so the
+                // no-slash form would list a redirect in every sitemap entry.
+                const canonicalPath = pagePath.endsWith('/') ? pagePath : `${pagePath}/`;
+                const loc = `${siteUrl.replace(/\/$/, '')}${basePath.replace(/\/$/, '')}${canonicalPath}`;
                 const gitDate = Git.getLastModified(page);
                 const lastmod = gitDate
                     ? gitDate.split('T')[0]
diff --git a/dist/react/Head.js b/dist/react/Head.js
index 14b82a21ac7f3f1f1cd8871d7cd1d5e73775ad71..04e3f1638442ccb1944ea571168680f873e10d51 100644
--- a/dist/react/Head.js
+++ b/dist/react/Head.js
@@ -14,7 +14,10 @@ export function Head() {
     const fullTitle = titleTemplate ? titleTemplate.replace('%s', title) : title;
     const description = frontmatter?.description ?? config.description;
     const fullPathname = basePath && basePath !== '/' ? `${basePath}${pathname}` : pathname;
-    const canonicalUrl = baseUrl ? `${baseUrl}${fullPathname}` : undefined;
+    // Trailing-slash form: splits.org canonicalizes to it, so canonical and
+    // og:url must not point at the no-slash URL it redirects away from.
+    const canonicalPathname = fullPathname.endsWith('/') ? fullPathname : `${fullPathname}/`;
+    const canonicalUrl = baseUrl ? `${baseUrl.replace(/\/$/, '')}${canonicalPathname}` : undefined;
     const ogImageTemplate = (() => {
         if (typeof ogImageUrl === 'function')
             return ogImageUrl(pathname, { baseUrl });
diff --git a/dist/react/internal/Outline.js b/dist/react/internal/Outline.js
index 4c3f4b9d933367f73b745288ae77e8f46d547932..11399b6155758f157599ec66a0e7841f33a1320d 100644
--- a/dist/react/internal/Outline.js
+++ b/dist/react/internal/Outline.js
@@ -196,7 +196,7 @@ function Items(props) {
         const observer = new ResizeObserver(measurePositions);
         observer.observe(container);
         return () => observer.disconnect();
-    }, []);
+    }, [items]);
     const indicatorStyle = React.useMemo(() => {
         if (activeIds.size === 0 || positions.size === 0)
             return { transform: 'translateY(0)', height: 0 };
diff --git a/dist/react/internal/Sidebar.js b/dist/react/internal/Sidebar.js
index 242cf4f6f5babe929912b71b3a6476bf66c3be57..aa599660d3434590b31345e3ee086502f4d262d4 100644
--- a/dist/react/internal/Sidebar.js
+++ b/dist/react/internal/Sidebar.js
@@ -235,14 +235,16 @@ function Section(props) {
             return false;
         if (hasActiveChildItem)
             return false;
+        if (groupLinkIsActive)
+            return false;
         if (props.disabled)
             return false;
         return Boolean(props.collapsed);
     });
     React.useEffect(() => {
-        if (hasActiveChildItem)
+        if (hasActiveChildItem || groupLinkIsActive)
             setCollapsed(false);
-    }, [hasActiveChildItem]);
+    }, [hasActiveChildItem, groupLinkIsActive]);
     const collapsable = typeof props.collapsed === 'boolean' && !props.disabled;
     const onCollapseInteraction = React.useCallback((event) => {
         if ('key' in event && event.key !== 'Enter')
@@ -252,7 +254,7 @@ function Section(props) {
     if (items)
         return (_jsxs("section", { "data-collapsed": collapsed, "data-v-sidebar-section": true, children: [(() => {
                     if (text && link)
-                        return (_jsxs("div", { className: depth > 0 ? Section.childHeaderClassName : Section.rootHeaderClassName, "data-condensed": condensed && depth > 1, "data-collapsed": collapsed, "data-collapsable": collapsable, "data-v-sidebar-section-header": true, ...(groupLinkIsActive && { 'data-active': true }), children: [_jsx(Link, { className: Section.headerLinkClassName, onClick: onNavigate, to: link, children: _jsxs("span", { className: "vocs:inline-flex vocs:items-center vocs:gap-1", children: [text, groupLinkIsExternal && _jsx(LucideArrowUpRight, { className: "vocs:size-3" })] }) }), badge && _jsx(ItemBadge, { badge: badge }), collapsable && (_jsx("button", { "aria-expanded": !collapsed, "aria-label": `Toggle ${text} section`, className: "vocs:ml-2 vocs:-mr-1 vocs:shrink-0 vocs:rounded-md vocs:p-1 vocs:text-secondary/80 vocs:hover:text-heading", onClick: () => setCollapsed((x) => !x), type: "button", children: _jsx(LucideChevronRight, { className: "vocs:data-collapsed:rotate-90 vocs:transition-transform vocs:duration-200 vocs:ease-in-out", ...(!collapsed ? { 'data-collapsed': false } : {}) }) }))] }));
+                        return (_jsxs("div", { className: depth > 0 ? Section.childHeaderClassName : Section.rootHeaderClassName, "data-condensed": condensed && depth > 1, "data-collapsed": collapsed, "data-collapsable": collapsable, "data-v-sidebar-section-header": true, ...(groupLinkIsActive && { 'data-active': true }), children: [_jsx(Link, { className: Section.headerLinkClassName, onClick: (event) => { setCollapsed(false); onNavigate?.(event); }, to: link, children: _jsxs("span", { className: "vocs:inline-flex vocs:items-center vocs:gap-1", children: [text, groupLinkIsExternal && _jsx(LucideArrowUpRight, { className: "vocs:size-3" })] }) }), badge && _jsx(ItemBadge, { badge: badge }), collapsable && (_jsx("button", { "aria-expanded": !collapsed, "aria-label": `Toggle ${text} section`, className: "vocs:ml-2 vocs:-mr-1 vocs:shrink-0 vocs:rounded-md vocs:p-1 vocs:text-secondary/80 vocs:hover:text-heading", onClick: () => setCollapsed((x) => !x), type: "button", children: _jsx(LucideChevronRight, { className: "vocs:data-collapsed:rotate-90 vocs:transition-transform vocs:duration-200 vocs:ease-in-out", ...(!collapsed ? { 'data-collapsed': false } : {}) }) }))] }));
                     // Non-link item is a header.
                     if (text)
                         return (_jsxs("div", { className: depth > 0 ? Section.childHeaderClassName : Section.rootHeaderClassName, "data-condensed": condensed && depth > 1, "data-collapsed": collapsed, "data-collapsable": collapsable, "data-v-sidebar-section-header": true, ...(collapsable
diff --git a/dist/react/internal/SkipToContent.client.js b/dist/react/internal/SkipToContent.client.js
index 8f1ab06a803496231d033e15e1bd0c8be8365e85..db7aaab19fa829deb4211e39ad4f5501c2e8129d 100644
--- a/dist/react/internal/SkipToContent.client.js
+++ b/dist/react/internal/SkipToContent.client.js
@@ -2,9 +2,13 @@
 import { jsx as _jsx } from "react/jsx-runtime";
 import { cx } from 'cva';
 import { useRouter } from 'waku';
+import { useConfig } from '../useConfig.js';
 export function SkipToContent(props) {
     const { className } = props;
+    const { basePath } = useConfig();
     const { path } = useRouter();
-    return (_jsx("a", { className: cx('vocs:fixed vocs:top-3 vocs:left-3 vocs:z-50', 'vocs:px-4 vocs:py-2', 'vocs:text-accent8 vocs:text-sm vocs:font-medium', 'vocs:bg-surface vocs:border-2 vocs:border-dashed vocs:border-accent8 vocs:rounded-lg', 'vocs:outline-none', 'vocs:opacity-0 vocs:pointer-events-none vocs:-translate-y-full', 'vocs:focus:opacity-100 vocs:focus:pointer-events-auto vocs:focus:translate-y-0', 'vocs:transition-all vocs:duration-150', className), "data-v-skip-to-content": true, href: `${path.split('#')[0]}#vocs-content`, children: "Skip to content" }));
+    const pagePath = path.split('#')[0];
+    const href = `${basePath === '/' ? '' : basePath.replace(/\/$/, '')}${pagePath}#vocs-content`;
+    return (_jsx("a", { className: cx('vocs:fixed vocs:top-3 vocs:left-3 vocs:z-50', 'vocs:px-4 vocs:py-2', 'vocs:text-accent8 vocs:text-sm vocs:font-medium', 'vocs:bg-surface vocs:border-2 vocs:border-dashed vocs:border-accent8 vocs:rounded-lg', 'vocs:outline-none', 'vocs:opacity-0 vocs:pointer-events-none vocs:-translate-y-full', 'vocs:focus:opacity-100 vocs:focus:pointer-events-auto vocs:focus:translate-y-0', 'vocs:transition-all vocs:duration-150', className), "data-v-skip-to-content": true, href, children: "Skip to content" }));
 }
 //# sourceMappingURL=SkipToContent.client.js.map
\ No newline at end of file
diff --git a/dist/waku/internal/middleware/md-router.d.ts b/dist/waku/internal/middleware/md-router.d.ts
index b39df0b3c12c27fdcc3f37641df47da64dbc4728..13e10ec7f44512160246c6d24a3e78d4bcdfcd8f 100644
--- a/dist/waku/internal/middleware/md-router.d.ts
+++ b/dist/waku/internal/middleware/md-router.d.ts
@@ -1,5 +1,5 @@
 import type { MiddlewareHandler } from 'hono';
-export declare function fetchMarkdown(url: URL, assetPath: string, cookie?: string): Promise<string | null>;
+export declare function fetchMarkdown(url: URL, assetPath: string, cookie?: string, basePath?: string): Promise<string | null>;
 export declare function middleware(): MiddlewareHandler;
 export default middleware;
 //# sourceMappingURL=md-router.d.ts.map
\ No newline at end of file
diff --git a/dist/waku/internal/middleware/md-router.js b/dist/waku/internal/middleware/md-router.js
index 30da92d062a46a48e033f61ec498b93c9f7b0e6f..950aebd3cdac30bba467839f85bc77b01b99e513 100644
--- a/dist/waku/internal/middleware/md-router.js
+++ b/dist/waku/internal/middleware/md-router.js
@@ -11,6 +11,7 @@ async function resolveContent() {
     const pages = await Llms.getPagesFromDir(pagesDir);
     const openapiPages = await Llms.getOpenApiPages(config);
     return Llms.buildLlmsContent({
+        basePath: config.basePath,
         pages: [...openapiPages, ...pages],
         title: config.title,
         description: config.description,
@@ -19,7 +20,7 @@ async function resolveContent() {
         sidebar: config.sidebar,
     });
 }
-export async function fetchMarkdown(url, assetPath, cookie) {
+export async function fetchMarkdown(url, assetPath, cookie, basePath = '') {
     // Try reading from disk first (avoids self-fetch issues with deployment protection).
     try {
         const fs = await import('node:fs/promises');
@@ -31,7 +32,9 @@ export async function fetchMarkdown(url, assetPath, cookie) {
     }
     catch { }
     // Fall back to HTTP fetch, forwarding cookies for auth-protected deployments.
-    const assetUrl = new URL(assetPath, url.origin);
+    // Static output is served under the base path, so the self-fetch has to
+    // carry it even though the on-disk path does not.
+    const assetUrl = new URL(`${basePath}${assetPath}`, url.origin);
     const headers = {};
     if (cookie)
         headers['cookie'] = cookie;
@@ -69,9 +72,19 @@ async function hasPublicFile(pathname) {
 export function middleware() {
     return async (context, next) => {
         const url = new URL(context.req.url);
+        // Requests arrive under the base path, while twins, page paths, and
+        // public files are all keyed off the output root. Resolve against the
+        // base-path-relative pathname so twins stay reachable under a base path.
+        const Config = await import('../../../internal/config.js');
+        const config = await Config.resolve({ server: true });
+        const basePath = config.basePath.replace(/\/+$/, '');
+        const relative = basePath ? url.pathname.slice(basePath.length) : url.pathname;
+        const pathname = basePath && url.pathname.startsWith(basePath) && (relative === '' || relative.startsWith('/'))
+            ? relative || '/'
+            : url.pathname;
         // Generated markdown twins are static output; routing them back through
         // twin resolution would trigger a recursive self-fetch.
-        if (url.pathname.startsWith('/assets/md/'))
+        if (pathname.startsWith('/assets/md/'))
             return next();
         const userAgent = context.req.header('user-agent') ?? '';
         const isOgBot = ogBotUserAgents.some((agent) => userAgent.includes(agent));
@@ -82,14 +95,14 @@ export function middleware() {
         const isTerminal = terminalUserAgents.some((agent) => userAgent.includes(agent));
         const acceptHeader = context.req.header('accept') ?? '';
         const acceptsMarkdown = acceptHeader.includes('text/markdown');
-        if (url.pathname === '/' && (acceptsMarkdown || isTerminal) && !isSearchEngine) {
+        if (pathname === '/' && (acceptsMarkdown || isTerminal) && !isSearchEngine) {
             let text;
             if (isDev) {
                 const content = await resolveContent();
                 text = content.short;
             }
             else {
-                text = await fetchMarkdown(url, '/llms.txt', context.req.header('cookie'));
+                text = await fetchMarkdown(url, '/llms.txt', context.req.header('cookie'), basePath);
             }
             if (!text)
                 return next();
@@ -100,19 +113,19 @@ export function middleware() {
             });
             return;
         }
-        const isMarkdownRequest = url.pathname.endsWith('.md');
+        const isMarkdownRequest = pathname.endsWith('.md');
         // A file physically present in `public/` wins over markdown-twin resolution,
         // so static `.md` files (skill manifests, plain markdown) are served as-is.
-        if (isMarkdownRequest && (await hasPublicFile(url.pathname)))
+        if (isMarkdownRequest && (await hasPublicFile(pathname)))
             return next();
         // Static assets (`.json`, `.svg`, `.png`, ...) have no markdown twin. Skip
         // twin resolution so a disk miss never falls back to a slow self-fetch.
-        const filename = url.pathname.split('/').pop() ?? '';
+        const filename = pathname.split('/').pop() ?? '';
         if (!isMarkdownRequest && filename.includes('.'))
             return next();
         if (!isMarkdownRequest && (isSearchEngine || (!isAiAgent && !isTerminal && !acceptsMarkdown)))
             return next();
-        const pagePath = url.pathname.replace(/\.md$/, '').replace(/\/index$/, '');
+        const pagePath = pathname.replace(/\.md$/, '').replace(/\/index$/, '');
         let text;
         if (isDev) {
             const content = await resolveContent();
@@ -120,10 +133,10 @@ export function middleware() {
             text = result?.content ?? null;
         }
         else {
-            const assetPath = url.pathname.endsWith('.md')
-                ? `/assets/md${url.pathname}`
-                : `/assets/md${url.pathname}.md`;
-            text = await fetchMarkdown(url, assetPath, context.req.header('cookie'));
+            const assetPath = pathname.endsWith('.md')
+                ? `/assets/md${pathname}`
+                : `/assets/md${pathname}.md`;
+            text = await fetchMarkdown(url, assetPath, context.req.header('cookie'), basePath);
         }
         if (!text)
             return next();
diff --git a/dist/waku/internal/patches/adapters/vercel-build-enhancer.js b/dist/waku/internal/patches/adapters/vercel-build-enhancer.js
index 5d48b823e98622768181e01b6330402c2c0f4343..02585282947b2d2ee0e1b141b685d9d2edde1390 100644
--- a/dist/waku/internal/patches/adapters/vercel-build-enhancer.js
+++ b/dist/waku/internal/patches/adapters/vercel-build-enhancer.js
@@ -5,7 +5,7 @@ function escapeRegExp(value) {
     return value.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
 }
 function markdownRoutes({ assetsDir, basePath, rscBase, }) {
-    const destination = basePath + rscBase + '/';
+    const destination = '/' + rscBase + '/';
     const markdownSource = `^${basePath}(?!${assetsDir}/)(.*\\.md)$`;
     const cleanPageSource = `^${basePath}(?!${assetsDir}/)(?!.*\\.[^/]+$)(.*)$`;
     const markdownUserAgentPattern = `.*(?:${[...aiUserAgents, ...terminalUserAgents]
@@ -83,6 +83,7 @@ export default getRequestListener(
     const routes = [
         {
             src: `^${basePath}${assetsDir}/(.*)$`,
+            dest: `/${assetsDir}/$1`,
             headers: {
                 'cache-control': 'public, immutable, max-age=31536000',
             },
@@ -92,9 +93,13 @@ export default getRequestListener(
                 ...markdown.beforeFilesystem,
                 { handle: 'filesystem' },
                 ...markdown.afterFilesystem,
+                {
+                    src: `^${basePath.replace(/\/$/, '')}$`,
+                    dest: '/' + rscBase + '/',
+                },
                 {
                     src: basePath + '(.*)',
-                    dest: basePath + rscBase + '/',
+                    dest: '/' + rscBase + '/',
                 },
             ]
             : []),
